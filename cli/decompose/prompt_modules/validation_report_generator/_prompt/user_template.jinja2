Now, here is the constraint/requirement for which you must design
a validation report schema:

<constraint_requirement>
{{ constraint_requirement }}
</constraint_requirement>

Please return the validation report schema inside the
<validation_report> tags as a JSON-like object that follows the
instructions in the system prompt.